const greeting = 'hello from plain JS';
console.log(greeting);

// "no-undef" must remain active for JavaScript files (it is turned off only for TypeScript files)
// eslint-disable-next-line no-undef
console.log(someUndeclaredVariable);
